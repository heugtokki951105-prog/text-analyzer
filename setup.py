from setuptools import setup, find_packages

setup(
    name="text_analyzer",
    version="0.1.0",
    author="박윤준",
    description="A simple Python package for text and social media analysis.",
    packages=find_packages(),
    install_requires=[],
)
